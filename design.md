# Video Learning Companion - Design Document

## 1. 设计动机

### 1.1 问题背景

传统的视频学习方式存在以下问题：
- **信息过载**：长视频中大量内容是冗余的，真正有价值的信息可能只有几个关键片段
- **被动接收**：用户只能被动观看，无法与视频内容进行深度互动
- **知识碎片化**：看完视频后，难以将零散的知识点整合成结构化的经验
- **成本高昂**：如果让AI看完整视频，Token消耗巨大，成本不可控

### 1.2 核心洞察

**"廉价理解"才是壁垒，不是"看视频"本身**

- ASR转写成本极低（约¥0.8/小时），可以快速获取视频的文本内容
- 视频理解模型成本较高（约¥7/百万Token），但可以精准分析关键片段
- **组合策略**：先用ASR获取全文，再按需用视频理解深入关键片段

### 1.3 设计理念

**"AI不是被动地看完整视频，而是按需深入关键片段"**

- 文本为主，画面为辅：ASR文本是主要依据，画面只是辅助验证
- 用户主导节奏：用户控制观看进度，AI按需响应
- 成本可控：总观看时长控制在视频时长的20-30%以内
- 知识沉淀：最终产出是结构化的知识经验，不是视频分析报告

## 2. 设计目标

### 2.1 核心目标

1. **一起看视频**：AI与用户共同观看学习型视频
2. **互动讨论**：用户随时提问，AI从文本定位并回答
3. **按需深入**：只在必要时截取片段进行视频理解
4. **沉淀经验**：讨论后生成结构化的知识总结

### 2.2 质量目标

| 指标 | 目标 | 说明 |
|:-----|:-----|:-----|
| 定位准确率 | ≥90% | AI从文本定位用户提到的时间段 |
| 画面判断准确率 | ≥80% | 正确判断是否需要看画面 |
| 成本控制 | ≤¥2/视频 | 20分钟视频的总成本 |
| 用户满意度 | 主观评估 | 用户觉得讨论有价值 |

### 2.3 体验目标

- **自然流畅**：像和朋友一起看视频讨论，不是机械地分析
- **按需深入**：不打断观看节奏，只在必要时深入
- **知识沉淀**：讨论后有实实在在的收获

## 3. 技术架构

### 3.1 整体架构

```
用户传入B站视频链接
        │
        ▼
① 下载视频（yt-dlp）
        │
        ▼
② ASR转写（fun-asr）
        │
        ▼
③ 用户边看视频边提问
        │
        ▼
④ AI从文本定位时间段
        │
        ▼
⑤ 判断是否需要看画面
        │
    ┌───┴───┐
    ▼       ▼
  不看     看画面
    │       │
    │   ⑥ 截取片段（ffmpeg）
    │       │
    │   ⑦ 视频理解（omni）
    │       │
    └───┬───┘
        ▼
⑧ 讨论并沉淀经验
```

### 3.2 技术栈

| 组件 | 技术 | 用途 |
|:-----|:-----|:-----|
| 视频下载 | yt-dlp | 下载B站视频（无需登录） |
| ASR转写 | 百炼 fun-asr | 语音转文字，带时间戳 |
| 视频理解 | 百炼 qwen3.5-omni-plus | 视频画面分析 |
| 文件存储 | 阿里云OSS | 上传大文件供模型读取 |
| 视频处理 | ffmpeg | 截取片段、格式转换 |

### 3.3 数据流

```
输入：B站视频链接
  │
  ├─→ yt-dlp下载 → video.mp4
  │
  ├─→ fun-asr转写 → transcript.txt
  │
  ├─→ 用户提问 → AI定位时间段
  │
  ├─→ ffmpeg截取 → clips/片段.mp4
  │
  ├─→ omni分析 → 片段.video_analysis.md
  │
  └─→ 讨论沉淀 → analysis_summary.md
```

## 4. 配置要求

### 4.1 阿里云百炼API

**只需要一个API Key**，但需要开通两个模型：

| 模型 | 用途 | 计费方式 |
|:-----|:-----|:---------|
| fun-asr | 语音转文字 | ¥0.00022/秒 |
| qwen3.5-omni-plus | 视频理解 | ¥7/百万Token（视频帧） |

**获取方式**：
1. 注册阿里云账号
2. 开通百炼服务
3. 获取API Key

### 4.2 阿里云OSS

**为什么需要OSS**：
- 百炼API要求文件通过URL访问
- 本地文件需要先上传到OSS，获取预签名URL
- 空桶不收费，存储费约¥0.12/GB/月

**配置信息**：
```bash
OSS_ACCESS_KEY_ID=your_access_key_id
OSS_ACCESS_KEY_SECRET=your_access_key_secret
OSS_ENDPOINT=oss-cn-beijing.aliyuncs.com
OSS_BUCKET=your_bucket_name
```

**获取方式**：
1. 在阿里云控制台创建OSS Bucket
2. 获取AccessKey ID和Secret
3. 配置Bucket权限（建议私有读写）

### 4.3 环境依赖

```bash
# Python依赖
pip install oss2 dashscope openai

# 系统工具
pip install yt-dlp  # 视频下载
brew install ffmpeg  # 视频处理（macOS）
```

### 4.4 配置方式

**方式1：环境变量（推荐）**
```bash
export DASHSCOPE_API_KEY="sk-xxxxx"
export OSS_ACCESS_KEY_ID="LTAIxxxxxx"
export OSS_ACCESS_KEY_SECRET="xxxxxx"
export OSS_BUCKET="your-bucket"
```

**方式2：修改脚本配置**
直接修改脚本中的默认值（不推荐，容易泄露密钥）

## 5. 边界情况与已知问题

### 5.1 ASR转写错误

**问题**：ASR可能将词语识别错误
- 例："没火" → "没货"
- 原因：发音相似、口语化、方言口音

**应对策略**：
- 当ASR文本语义不通顺时，可能是识别错误
- 建议截取该片段，用视频理解来验证
- 关键信息（人名、地名、专业术语）需要特别注意

**在SKILL中的体现**：
- "可以考虑看"的情况中包含"ASR文本令人困惑"

### 5.2 画面重要性判断

**问题**：如何判断是否需要看画面？

**三档判断标准**：

| 档位 | 判断依据 | 示例 |
|:-----|:---------|:-----|
| 必须看 | 文本明确提到视觉内容 | "大家看这个图表" |
| 可以考虑看 | 模型判断画面确实必须看一眼 | ASR文本不通顺、关键概念 |
| 不看（默认） | 纯口播、文本清晰 | 生活Vlog、情感表达 |

**核心原则**：只有模型判断"画面确实必须看一眼"时才截取

### 5.3 成本控制

**问题**：视频理解成本较高

**控制策略**：
- 只看关键片段，不看全视频
- 总观看时长控制在视频时长的20-30%以内
- 纯口播视频通常不需要看画面

**成本参考**：

| 视频长度 | 看片段数 | 预估成本 |
|:---------|:---------|:---------|
| 3分钟 | 0-1个 | ¥0-0.3 |
| 10分钟 | 1-3个 | ¥0.3-0.8 |
| 20分钟 | 2-4个 | ¥0.5-1.5 |

### 5.4 文件结构管理

**问题**：中间文件如何组织？

**解决方案**：
```
video-workspace/
└── {BV号}/
    ├── video.mp4                    # 原始视频
    ├── transcript.txt               # ASR文本（主要使用）
    ├── analysis_summary.md          # 汇总分析报告
    └── clips/
        ├── 01_描述.mp4
        ├── 01_描述.video_analysis.md
        ├── 02_描述.mp4
        └── 02_描述.video_analysis.md
```

**清理建议**：
- `transcript.json`：不需要，可以删除
- 定期清理clips目录中的临时片段
- 用户可手动清空video-workspace目录

### 5.5 视频下载问题

**问题**：yt-dlp下载可能失败

**可能原因**：
- 网络问题
- B站反爬机制（HTTP 412错误）
- 视频需要登录（大会员内容）

**应对策略**：
- 稍后重试
- 更新yt-dlp：`pip install --upgrade yt-dlp`
- 720p画质通常不需要登录

## 6. 维护指南

### 6.1 如何接手这个Skills

**第一步：理解核心理念**
- 阅读本design.md，理解设计动机和目标
- 核心是"按需深入"，不是"看完整视频"

**第二步：了解技术栈**
- yt-dlp：视频下载
- fun-asr：ASR转写
- qwen3.5-omni-plus：视频理解
- ffmpeg：视频处理

**第三步：配置环境**
- 获取阿里云百炼API Key
- 配置OSS Bucket
- 安装Python依赖和系统工具

**第四步：测试验证**
- 用短视频（3-5分钟）测试完整流程
- 验证下载、ASR、视频理解各环节
- 记录遇到的问题和改进建议

### 6.2 如何修改和扩展

**修改提示词**：
- 编辑`analyze_video.py`中的`DEFAULT_PROMPT`
- 根据不同视频类型定制提示词
- 测试修改后的效果

**扩展功能**：
- 支持更多视频平台（YouTube、抖音等）
- 批量处理多个视频
- 知识图谱整合
- 复习提醒功能

**优化性能**：
- 优化ASR转写速度
- 优化视频理解成本
- 优化文件管理流程

### 6.3 常见问题排查

**Q1: ASR转写失败**
- 检查网络连接
- 检查API Key是否正确
- 检查OSS Bucket权限

**Q2: 视频理解失败**
- 检查视频文件是否损坏
- 检查文件大小是否超过限制（2GB）
- 检查API Key是否有权限

**Q3: 下载视频失败**
- 检查网络连接
- 更新yt-dlp
- 检查视频是否需要登录

## 7. 未来演进

### 7.1 短期优化（1-2周）

- [ ] 支持更多视频平台（YouTube、抖音）
- [ ] 优化提示词，提高分析质量
- [ ] 添加批量处理功能

### 7.2 中期扩展（1-2月）

- [ ] 知识图谱整合
- [ ] 复习提醒功能
- [ ] 支持更多视频类型（教程、会议、播客等）

### 7.3 长期愿景（3-6月）

- [ ] 实时视频分析（边看边分析）
- [ ] 多人协作学习
- [ ] 与笔记工具集成（飞书、Notion等）

## 8. 参考资料

### 8.1 官方文档

- [百炼API文档](https://help.aliyun.com/zh/model-studio/)
- [OSS文档](https://help.aliyun.com/zh/oss/)
- [yt-dlp文档](https://github.com/yt-dlp/yt-dlp)

### 8.2 相关Skills

- `ark-vision`：火山引擎视觉理解
- `lark-doc`：飞书文档操作
- `lark-drive`：飞书云空间管理

### 8.3 技术参考

- ASR转写：fun-asr模型文档
- 视频理解：qwen3.5-omni-plus模型文档
- 视频处理：ffmpeg官方文档

---

**最后更新**：2026-06-06

**维护者**：Jack (Video Learning Companion Team)
